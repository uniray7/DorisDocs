# OLAP Platform 產品規劃摘要

> 對象：管理層與 PM ｜ 日期：2026-07-13 ｜ 狀態：規劃中（PRD 細化階段，功能規格完成 5/10）
> 詳細規格見 `internal/PRD.md` 與 `internal/feature-specs/`；本文為決策導向摘要。

---

## 一、我們要做什麼

以 **Apache Doris** 為基礎，提供公司內部的**多租戶 OLAP 分析服務**，並附帶 **data ingestion pipeline** 讓各部門把資料灌進來分析。

**為什麼做**：擺脫 Oracle license 成本與綁定、sunset HBase 舊 tech stack。既有分析型資料規模約數百 TB。

**明確不做**：既有資料遷移（各團隊自理）、跨部門資料分享（lakehouse 層解決）、交易型（OLTP）工作負載、BI 工具託管。

---

## 二、產品形狀：兩種服務模式

這是整個產品最核心的設計——**責任邊界以服務模式劃分**，申請時選定、**不可事後轉換**：

| | **Managed** | **Self-managed** |
|---|---|---|
| 適合對象 | 想省事、接受平台規範的團隊 | 高客製需求、大流量、有能力自管的團隊 |
| 資料寫入 | 只走平台 pipeline（streaming CDC / batch） | 一律自建 pipeline 自行寫入 |
| 建表/改表 | 走申請流程，平台審核後代為執行 | 完全自由 |
| 資料管理責任 | **平台承擔**：備份還原、保留清除、效能調校、schema 品質把關 | **使用者自負** |
| 平台支援 | 完整 | 僅系統異常告警 + 付費代操作（scale in/out） |
| 可用規格 | Shared 或 Dedicated cluster | 限 Dedicated cluster（tier 2 起） |

## 三、資源分級（Tier）

| Tier | 資料量 | 峰值 QPS | 配置 | 可用模式 |
|------|--------|----------|------|----------|
| 1 | < 500GB | < 20 | 共用 cluster（資源隔離） | 僅 managed |
| 2 | 500GB–5TB | 20–100 | 專屬小型 cluster（3 BEs） | 兩者皆可 |
| 3 | 5TB–30TB | 100–500 | 專屬中型 cluster（5–10 BEs） | 兩者皆可 |
| 超過 | >30TB 或 >500 QPS | — | **個案洽談（處理方式待定）** | — |

> ⚠️ 門檻數字為草案。「資料量與 QPS 綁死在同一 tier」已知不合理（例：資料小但查詢重的團隊無法歸類），已規劃 RFC 討論儲存/運算解耦。硬體池浮動（依採購狀況），容量規劃以單一租戶資源模型描述。

## 四、使用者旅程

```
填投影片 template → 每週三申請會議報告 → 審核（tier 判定 + schema 審查 + 成本中心確認）
  → Staging 試用環境驗證（小規格，驗設計不驗效能）→ 平台佈建正式環境 → 上線
```

- **申請流程純人工**（投影片 + 會議 + 追蹤表），不建自動化系統；申請量成長後再依已定義的狀態機做自助入口。
- **Staging 是試用性質**：固定小規格（機器不足以對齊正式規格），驗證 schema/查詢設計合理性；效能數據僅供參考，**不構成平台效能承諾**。

## 五、Managed 模式的資料管理

- **三層 zone**（對應 Databricks Bronze/Silver/Gold）：`tmp`（CDC 原始落地，平台管理）→ `raw`（結構化資料，**建表需平台嚴審**）→ `curated`（使用者 ELT 自產，免審）。
- **Streaming pipeline**：使用者以固定 format 打 message 進平台 Kafka，支援三種 format（generic CDC / DB2 JSON CDC / DB2 XML CDC）；格式錯誤的資料進 corrupted data 區（MinIO）並告警，不中斷匯入。
- **Batch pipeline**：規格細化中；介面預留未來 lakehouse → Doris 通道的擴充。
- **高可用**：active-standby 架構 + cross-cluster replication + failover（具體 RPO/RTO 待量化）。

## 六、已定案的關鍵決策（節錄）

| 決策 | 內容 |
|------|------|
| 租戶單位 | Workspace（團隊自定粒度），**強制綁定成本中心** |
| 隔離要求 | 資料、metadata、運算三層完全隔離；**不支援跨 ws 分享**（與 lakehouse 習慣不同，需對使用者前置溝通） |
| 模式轉換 | 不可轉換；要換模式需開新 ws 搬資料 |
| 計量方式 | 儲存：審核用原始大小、上線後按壓縮後占用；QPS：5 分鐘滑動窗口平均 |
| 命名慣例 | Managed 一律 `{ws}__{自訂}__{zone}`；self-managed 命名自由 |
| 對外文件深度 | Managed 須講明使用規範+限制+責任歸屬；self-managed 重點講責任歸屬 |

## 七、需要管理層/PM 拍板或注意的事項

### 待拍板（會影響產品走向）
| # | 事項 | 影響 | 建議時點 |
|---|------|------|----------|
| 1 | **計費模式**：chargeback 實收 vs showback 僅報表 vs 免費內部服務 | 影響成本中心綁定的意義、超額處理、self-managed 代操作收費 | NFR 定案前（近期） |
| 2 | **Tier 門檻與儲存/運算解耦**（RFC） | 影響定價與硬體採購規劃 | RFC 討論 |
| 3 | **超過 tier 3 的大型需求**如何承接 | 是否為此擴 pool、或明確拒收 | 首個大戶出現前 |
| 4 | **超標但不願升級**的處理：限流維持 vs 強制升級 | 與計費模式連動 | 與 #1 一併 |
| 5 | **Row/column filter**（細粒度權限）做不做在平台側 | 使用者已提需求；做則增加範圍 | RFC 討論 |
| 6 | **PII/機敏資料的平台責任範圍** | 影響審核流程與合規成本 | 治理審查階段 |
| 7 | **時程與首個目標部門** | 目前皆未定 | 越早越好 |

### 需注意的風險
- **人工申請流程的規模瓶頸**：每週三會議承載量有限，申請量成長時會排隊；已預留自動化路徑，但需要有觸發升級的判斷點。
- **Staging 與正式環境落差**：staging 驗不了真實效能，上線後才見真章——對外文件會明確免責，但第一批使用者的預期管理要做好。
- **硬體池浮動**：tier 2/3 佈建可能因採購進度出現「等待資源」狀態，影響開通 lead time 的承諾。
- **Lakehouse 使用習慣落差**：使用者習慣跨 workspace 存取，本平台明確禁止——對外溝通不足會成為第一波客訴來源。

## 八、文件進度與下一步

| 項目 | 狀態 |
|------|------|
| PRD 大綱 + 術語 + 範圍 | ✅ 完成 |
| 功能規格（10 項） | 進行中 5/10：申請流程、staging、多租戶隔離、tier 分配、streaming pipeline 已完成草稿 |
| 未完成規格 | batch pipeline、database/table 申請、self-managed 營運支援、配額可視化、帳號權限 |
| 後續階段 | 邊界審查（QA 視角）→ NFR/SLA 量化 → 治理合規 → 利害關係人會簽 → 產出架構文件與對外文件 |
